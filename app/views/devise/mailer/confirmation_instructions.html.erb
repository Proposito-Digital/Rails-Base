<p><%=t("emails.confirmation_instructions.title", locale: locale, email: @resource.email)%>!</p>

<p><%=t("emails.confirmation_instructions.can_confirm", locale: locale)%></p>

<p><%= link_to t("emails.confirmation_instructions.link", locale: locale), confirmation_url(@resource, confirmation_token: @token) %></p>
