<ul class="navbar-nav">
  <li class="nav-item dropdown">
    <a class="nav-link dropdown-toggle" href="#" id="dropdown05" data-bs-toggle="dropdown" aria-expanded="false"><%= current_user.name %></a>
    <%= render 'layouts/admin/shared/avatar' %>
    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
      <a class="dropdown-item" href="<%= edit_admin_user_path(current_user.id) %>"> Editar Perfil</a>
      <a class="dropdown-item" data-bs-toggle="modal" data-bs-target="#modal-change-password">Alterar Senha</a>
      <%= link_to 'Sair', destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" %>
    </ul>
  </li>
</ul>